name: shopping
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Qr code generation
  qr_flutter: ^4.1.0

  # Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Barcode scanning
  mobile_scanner: ^5.2.3

  # Sensor beyond GPS/Camera
  battery_plus: ^6.0.1

  # To show notifications
  flutter_local_notifications: ^17.2.2

  # Analytics
  firebase_core: ^3.6.0
  firebase_analytics: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
